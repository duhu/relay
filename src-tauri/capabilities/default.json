{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the settings and log windows may use; Relay's own commands need no permission.",
  "windows": ["settings", "log"],
  "permissions": ["core:default", "dialog:allow-save", "dialog:allow-open"]
}
